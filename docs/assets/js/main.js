"use strict";const searchInput=document.querySelector(".js-input"),searchBtn=document.querySelector(".js-btn"),cardsList=document.querySelector(".js-list"),favCardsList=document.querySelector(".js-fav-list");let charactersList=[],favouriteCharacters=[];function renderCards(r){return`<li class="cards-list-item">\n    <article class="card js-card" id="${r.char_id}">\n      <img src="${r.img}" alt="Picture of ${r.name}" title="${r.name}" class="card-img" />\n      <h3 class="card-name">${r.name}</h3>\n      <p class="card-status">${r.status}</p>\n    </article>\n  </li>`}function renderCharactersList(){let r="";for(const a of charactersList)r+=renderCards(a);cardsList.innerHTML=r,cardListeners()}function renderFavCard(r){return`<li class="cards-list-item">\n    <article class="card js-card selected" id="${r.char_id}">\n      <img src="${r.img}" alt="Picture of ${r.name}" title="${r.name}" class="card-img" />\n      <h3 class="card-name">${r.name}</h3>\n      <p class="card-status">${r.status}</p>\n    </article>\n  </li>`}function renderFavCharacters(){let r="";for(const a of favouriteCharacters)r+=renderFavCard(a);favCardsList.innerHTML=r}function handleClickCard(r){r.currentTarget.classList.toggle("selected");const a=parseInt(r.currentTarget.id),e=charactersList.find(r=>r.char_id===a),t=favouriteCharacters.findIndex(r=>r.char_id===a);-1===t?favouriteCharacters.push(e):favouriteCharacters.splice(t,1),renderFavCharacters()}function cardListeners(){const r=document.querySelectorAll(".js-card");for(const a of r)a.addEventListener("click",handleClickCard)}function getData(){fetch("https://breakingbadapi.com/api/characters").then(r=>r.json()).then(r=>{charactersList=r,renderCharactersList()})}function handleSearch(){}searchBtn.addEventListener("click",handleSearch),getData();