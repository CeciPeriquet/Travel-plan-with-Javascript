"use strict";const searchInput=document.querySelector(".js-input"),searchBtn=document.querySelector(".js-btn"),cardsList=document.querySelector(".js-list");let charactersList=[];function renderCards(e){return`<li class="cards-list-item">\n    <article class="card js-card">\n      <img src="${e.img}" alt="Picture of ${e.name}" title="${e.name}" class="card-img" />\n      <h2 class="card-name">${e.name}</h2>\n      <p class="card-status">${e.status}</p>\n    </article>\n  </li>`}function renderCharactersList(){let e="";for(const t of charactersList)e+=renderCards(t);cardsList.innerHTML=e,cardListeners()}function handleClickCard(e){console.log("holi")}function cardListeners(){const e=document.querySelectorAll(".js-card");for(const t of e)t.addEventListener("click",handleClickCard)}function getData(){fetch("https://breakingbadapi.com/api/characters").then(e=>e.json()).then(e=>{charactersList=e,renderCharactersList()})}function handleSearch(){}searchBtn.addEventListener("click",handleSearch),getData();